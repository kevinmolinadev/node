describe("App", () => {
    test("should be 50", () => {
        const number = 20;
        const numberTwo = 30;

        const result = number + numberTwo;
        const expectedResult = 50;

        expect(result).toBe(expectedResult);
    });
});
